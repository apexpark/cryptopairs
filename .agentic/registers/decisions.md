# Decisions Register

Machine-of-record for Operator decisions that change authority, scope, risk
posture, or process. Append-only; supersede by adding a new row and flipping
the old row's status. Protected-path merge authorizations are recorded here
with head SHA and merge SHA.

| Date | Decision | Source | Status |
|---|---|---|---|
| 2026-07-12 | Adopt the apex-forge dual-agent governance scaffold (v0) in cryptopairs. Install the full `.agentic/` harness by building on the existing `codex/agentic-loop-harness-adapter` branch (build-on, not supersede), on lane branch `claude/agentic-scaffold-v0`. Installation follows the process it installs: inner review, PR, Codex exact-SHA review, Operator authorization. | Operator instruction, Claude Code session, step card OP-1 | active |
| 2026-07-12 | Merge authority tiers: (1) docs/chore — Lead Coder merges after green CI, reports after; (2) code outside protected paths — Lead Coder merges after clean multi-angle inner review + green CI; (3) protected paths — Independent Reviewer (assignment per §Roles / OP-44) clean review at exact head SHA + green CI + Operator authorization on a plain-English brief; stale-SHA verdicts void; fresh review after every repair push; (4) live capital, risk limits, paper→live toggle, Hetzner production runtime — Operator only, never delegated. Tiers 1–2 delegated merge is adopted but **not operative** until the workflow-manual slice amends `docs/ops/ai_workflow.md` and the PR template; until then every merge requires per-PR Operator authorization. | Operator answer OP-1 Q2 (approved recommended tiers) | superseded (see 2026-07-12 restated-tiers row) |
| 2026-07-12 | Protected paths (Tier 3), full recommended list: `services/execution-service/**`; `specs/contracts/**` and `specs/examples/**`; `scripts/deploy.sh` and `infra/**`; governance files (`AGENTS.md`, `CLAUDE.md`, `.agentic/**`, `.github/**`, `docs/00-guardrails.md`, `docs/11-data-integrity-policy.md`, `docs/12-risk-and-execution-policy.md`, `docs/13-secrets-and-security.md`); champion promotion/selection modules in `services/strategy-service`; `tools/scripts/autopilot_*.py`. `.github/CODEOWNERS` becomes the single source of truth once the CODEOWNERS expansion slice merges; until then this row is binding. | Operator answer OP-1 Q1 (approved full recommended list) | superseded (see 2026-07-12 expanded protected-path row) |
| 2026-07-12 | Safety invariants for the Autonomy Doctrine: kill switch halts all new order submissions and is never bypassed; fail closed on missing/stale data or unknown risk state; live `ENTRY`/`EXIT` requires explicit operator confirmation; champion promotions are operator-triggered only; `STRATEGY_BLOCK_ON_CHAMPION_DRIFT=true` stays set; no unattended loops except operator-scheduled bounded one-shot scripts; emergency stop-close (capital protection) is always automated once a position exists per `docs/12` rule 9, while discretionary live `EXIT` intents still require operator confirmation per `docs/12` rule 8 — autonomy restrictions never apply to capital protection. Doctrine graduation maps to the native AUTO ladder (AUTO-2A → AUTO-2B → AUTO-2C → AUTO-3) rather than a parallel tier scheme. | Operator answer OP-1 Q3 (approved recommended set) | superseded (ladder restated with AUTO-2D; see 2026-07-12 doctrine-ladder row) |
| 2026-07-12 | Defer extraction of the generic `apexpark/agentic-scaffold` template repo until this installation has survived real use. | Operator answer OP-1 Q4 | active |
| 2026-07-12 | Merge authorization, PR #245 (scaffold v0 install): Codex CLEAN at head SHA `7c0efe2000ed69651db3ce19ca9ce687e4e4b1db` after three exact-SHA review cycles (`44f40b5` CLEAN → voided by repair push; `e3dfbde` FINDINGS → repaired; `7c0efe2` CLEAN); Operator executed the squash merge personally (`gh pr merge 245 --squash --admin`), landing as `2516fc5dcf4d030b32684ad87257301e59d7dc89`. | Operator step cards OP-3/OP-6 and personal merge execution | done |
| 2026-07-12 | Standing delegated merge, Tiers 1–2: the Lead Coder may execute the mechanical squash merge for Tier 1 (docs/chore, green CI) and Tier 2 (code outside protected paths, clean multi-angle inner review + green CI). Scope of delegated tooling: exactly `gh pr merge <N> --squash --delete-branch` on a qualifying PR, with `--admin` permitted solely to satisfy the approval formality GitHub cannot path-scope, and only after the Lead Coder has verified via `gh pr checks` that every required check passes and via `gh pr view` that the head SHA equals the inner-reviewed SHA. Merging over failing, pending, or bypassed checks, or with unresolved review threads, is forbidden even when delegated. Record and report per merge: at merge time post a merge-record comment on the PR (tier claimed, head SHA verified, checks state, inner-review evidence) and report to the Operator in the same session or at the next Operator interaction — batching or deferring reports is forbidden. Other conditions: exact-SHA discipline; fresh review after every repair push; ambiguous tier → treat as the higher tier; revocable at any time by Operator instruction. Forbidden even when delegated: all Tier 3 protected paths, all Tier 4 surfaces, contract or schema changes, deploys, secrets, live/paper toggles, branch-protection changes, force-push, and — independent of the protected-path list — operator runbooks (`docs/playbooks/**`), git hooks (`.githooks/**`), `scripts/**`, `docker-compose*.yml`, and dependency/toolchain manifests (`Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`). Operative upon merge of the workflow-manual amendment slice (GOV-SCAFFOLD-2). | Operator answer OP-7 ("Go", including the item-4 standing delegation); hardened per adversarial inner review, ratified OP-8 | active |
| 2026-07-12 | Merge authority tiers (restated, operative upon merge of the GOV-SCAFFOLD-2 slice; definitions unchanged from the superseded OP-1 Q2 row): (1) docs/chore — delegated mechanical merge after green CI; (2) code outside protected paths — delegated mechanical merge after clean multi-angle inner review + green CI; (3) protected paths — the Independent Reviewer (assignment per §Roles / OP-44 — Codex before OP-44, Claude after) CLEAN at exact head SHA + green CI + Operator authorization on a plain-English brief; (4) live capital, risk limits, paper→live toggle, Hetzner production runtime — Operator only, never delegated. Ambiguous tier → treat as the higher tier. | OP-1 Q2 as made operative by OP-7/OP-8 | active |
| 2026-07-12 | Protected paths (Tier 3), expanded list superseding the OP-1 Q1 row after adversarial inner review and Codex PR #246 review: all `services/**`; `specs/contracts/**` and `specs/examples/**`; `scripts/**` and `infra/**`; `docs/playbooks/**`; `.githooks/**`; `docker-compose*.yml`; `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`; `.env.example`; governance files (`AGENTS.md`, `CLAUDE.md`, `.agentic/**`, `.github/**`, `docs/ops/**`, `docs/AGENT_STATE.md`, `docs/00-guardrails.md`, `docs/05-agent-build-workflow.md`, `docs/07-dependency-and-supply-chain-policy.md`, `docs/11-data-integrity-policy.md`, `docs/12-risk-and-execution-policy.md`, `docs/13-secrets-and-security.md`, `docs/17-verification-protocol.md`); `tools/scripts/autopilot_*.py`. Tier 2 therefore covers: `crates/**`, `apps/web/**`, `research/**`, `tools/**` (except autopilot scripts), `plans/**`, and remaining non-policy docs. Consequence for delegated merges: Tier 1–2 PRs must not touch `docs/AGENT_STATE.md`; their per-merge record comment is the immediate record, and state refresh happens in the next Tier 3 governance PR. `.github/CODEOWNERS` becomes the single source of truth once the CODEOWNERS expansion slice merges; until then this row is binding. | Adversarial inner review findings; Operator ratification OP-8 Q1 (full expansion) | active |
| 2026-07-12 | Tier 2 review standard confirmed: code outside protected paths merges on multi-angle inner review + green CI without cross-model Independent Reviewer review. This is an explicit Operator decision resolving the tension with the `AGENTS.md` §8.3 default work-split reading; cross-model review remains required for Tier 3. | Operator answer OP-8 Q2 | active |
| 2026-07-12 | Merge authorization, PR #246 (tiers operative + hardened delegation + expanded protected paths): Codex CLEAN at head SHA `053da1174353e9ab28953a3df2aa2af7b20955cb` after three exact-SHA review cycles (`08da4fe` FINDINGS → repaired; `8db76eb` FINDINGS → repaired; `053da11` CLEAN); Operator executed the squash merge personally, landing as `7041b41` on `main`. Tiers 1–2 delegated merge operative from this merge. | Operator step cards OP-9/OP-10/OP-11 and personal merge execution | done |
| 2026-07-12 | Autonomy Doctrine ladder (restating the superseded OP-1 Q3 row's mapping; invariants unchanged): graduation follows the AUTO-2 §3 non-negotiable sequence in full — AUTO-2A → AUTO-2B → AUTO-2C → **AUTO-2D (dynamic paper trial)** → AUTO-3 design gate — with AUTO-1 as the deployed observe-only predecessor. AUTO-2D must not be skipped before AUTO-3 without a recorded governance exception. Authoritative text: `CLAUDE.md` §Graduation and `docs/proposals/AUTO-2-1m-paper-autopilot-governance.md` §3. | Codex PR #247 review finding; sequence per AUTO-2 §3 (operator-ratified proposal) | active |
| 2026-07-12 | Merge authorization, PR #247 (CLAUDE.md Autonomy Doctrine): Codex CLEAN at head SHA `a5132ae25d0038763013367daf1c9fb46cf2a0e9` after four exact-SHA review cycles (`134e62e` FINDINGS → repaired; `9922fca` CLEAN with inline P2 thread → repaired; `a5132ae` CLEAN, confirmed by Operator OP-16); Operator executed the squash merge personally, landing as `b409849bd9b673958f5347d15be013c488e14974`. | Operator step cards OP-13/OP-14/OP-15, OP-16 verdict confirmation, and personal merge execution | done |
| 2026-07-12 | CODEOWNERS canonicalization (operative upon merge of the GOV-SCAFFOLD-4 slice): `.github/CODEOWNERS` becomes the single source of truth for Tier 3 protected paths, expanded to the full 2026-07-12 OP-8 list. Its global `*` line is review-routing only, not a Tier 3 designation; on any disagreement between protected-path lists the broader protection applies until reconciled (narrowing is a defect, never a relaxation). Extended per Codex PR #248 review: dependency manifests of every toolchain covered by `docs/07` (`package.json`, `package-lock.json`, `pyproject.toml`, `requirements.txt`, unanchored) are Tier 3 at any depth, including inside otherwise-Tier 2 trees (`apps/web/**`, `research/**`). It additionally retains the legacy pre-scaffold protections (`CHANGELOG.md`, `docs/02-versioning-and-releases.md`, `docs/03-contracts-and-compatibility.md`, `docs/15-observability-and-alerting.md`, `docs/16-ui-styling-guide.md`) — protections are never silently relaxed. Consequence: Tier 1–2 delegated PRs must not touch `CHANGELOG.md`; like `docs/AGENT_STATE.md`, its entries batch into the next Tier 3 governance PR. | Operator answer OP-16 ("Slice 4: Go"); legacy retention per no-relaxation rule | active |
| 2026-07-12 | Merge authorization, PR #248 (CODEOWNERS canonicalization): Codex CLEAN at head SHA `51baba1ca6e0036c03a7a76ec951bc90d3664af6` after three exact-SHA review cycles (`70eb211` FINDINGS → repaired; `1e6b4c9` CLEAN with unresolved threads → resolved and worker result recorded; `51baba1` CLEAN); Operator executed the squash merge personally, landing as `4bce8e5712ca23bbbe670bb870eb5e8159c0cc30`. Governance scaffold installation complete. | Operator step cards OP-17/OP-18 and personal merge execution | done |
| 2026-07-13 | AUTO-2A operator paper evidence accepted and AUTO-2B adoption: the 72h direction-gated trial (run `20260628T061640Z`, 83/83 closed positions, +288.99 net bps, 69% profitable, tail-loss caveat −118.05 bps on `PF_TAOUSD__PF_HYPEUSD`) satisfies the AUTO-2A §3 exit criteria. Proceed with AUTO-2B by adopting and refreshing the pre-migration PR #244 (`codex/auto2b-shadow-dynamic-allowlist`) under the Tier 3 flow, rather than restarting. Shadow output remains advisory only. | Operator answer OP-21 ("Proceed… once you have confirmed there isn't already progress"); prior-work confirmation in session | active |
| 2026-07-13 | Merge authorization, PR #244 (AUTO-2B shadow dynamic allowlist, adopted pre-migration work): Codex CLEAN at head SHA `bfc9a2b09d338843a2abe09aa3c15f6b6fa91d78` (cross-model dual review: Claude inner-reviewed the Codex-authored content, Codex exact-SHA-reviewed the refreshed head); Operator executed the squash merge personally, landing as `632ba80d31885a6427de68d93e2e003f95543b85`. | Operator step card OP-22 and personal merge execution | done |
| 2026-07-13 | OP-24: extend AUTO-2B to the full pair universe (design-proposal-first) and start the second 72h direction-gated paper window with the unchanged 4-leg static allowlist. Prior-work check condition satisfied (PR #244 adopted). | Operator answer OP-24 | active |
| 2026-07-13 | AUTO-2B.2 design proposal approved and merged: PR #249 (`docs/proposals/AUTO-2B2-full-universe-shadow.md`), Tier 1 by path with the standing delegation deliberately not exercised; Operator personally squash-merged as `c1eaf79` per AUTO-2 §6 (Operator approval required for AUTO-sequence slices). Selector-view capture design; no simulation layer; implementation slices B2-a..d each require the Tier 3 flow and Operator answers to the §8 open questions. | Operator step card OP-31 and personal merge execution | active |
| 2026-07-13 | Host actions (OP-25..OP-30, operator-executed on Hetzner): system reboot (pending OS restart applied); `docker update --restart unless-stopped` on the six cryptopairs containers after the reboot exposed missing restart policies; fresh observe capture run `20260713T060526Z` started (previous 15-day loop retired); second 72h paper window run `20260713T060641Z` started with a loop-resilience deviation from the paper runbook — a stale observe-freshness tick skips and logs instead of killing the loop (per-tick fail-closed unchanged) — explicitly authorized for this window and made official runbook text by this slice. | Operator execution of step cards OP-25..OP-30 | done |
| 2026-07-13 | OP-35: go on AUTO-2B.2 implementation, slice B2-a, with §8 answers — bucket scope: all three cue buckets; capture cadence: 300s; disk: operator-run estimate before any capture starts. Contract shape decision (B2-a per proposal §4.1): observe_record v2 splits into two row types via oneOf — entry rows keep the exact v1 shape (versions 1 and 2 both valid) and selector-view rows are their own type (`capture_profile: selector_view`, `decision: SELECTOR_VIEW_OBSERVED`, cue_bucket + selector-stated fields, no realized/outcome fields). | Operator answer OP-35 | active |
| 2026-07-13 | OP-44: swap the Claude/Codex role assignments for cryptopairs. Codex becomes **Lead Coder + Operator Interface** (authors slices, inner review, opens PRs, gives Operator briefs/step cards, records decisions); Claude becomes **Independent Reviewer** (read-only exact-SHA review of protected-path PRs). Model-diversity rule unchanged (reviewer model differs from author). Operative for the first slice started after this decision merges; AUTO-2B.2 B2-b (PR #252), in flight under the prior assignment (Claude author / Codex reviewer), completes under the prior assignment. Post-swap interaction: the Operator drives Codex to author/implement and receives Codex's briefs/step cards, and brings Codex's protected-path PRs to Claude for exact-SHA review. Adds `CODEX.md` as the Codex session entry point; the Autonomy Doctrine in `CLAUDE.md` is model-agnostic and binds whoever holds the Coder role. | Operator instruction (this session) | active |
