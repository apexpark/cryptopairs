# Context Pack — <work order id>

Minimum context for the worker. Assume nothing outside this pack and the
mandatory hydration reads (`AGENTS.md`, `docs/AGENT_STATE.md`).

## Read first (in order)

1.

## Current state

Branch, pin, relevant in-flight work, last relevant PR.

## Known constraints

Safety invariants that touch this task; forbidden surfaces; precedence notes.

## Operator-provided runtime context

Verbatim quotes only (host output, screenshots), each labeled with date and
source. Never paraphrased into fact.
